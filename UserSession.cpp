#include "Headers/UserSession.hpp"

/* UserSession::UserSession() {
    printf("%d",getUserType());
} */

/* UserType UserSession::getUserType() {
    if (getuid() == 0) {
        return ADMIN;
    }
    return CUSTOMER;
} */