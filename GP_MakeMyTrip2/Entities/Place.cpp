#include "Place.hpp"

Place::Place(const std::string &city,
             const std::string &state,
             const std::string &country)
    : city(city),
      state(state),
      country(country)
{
}
